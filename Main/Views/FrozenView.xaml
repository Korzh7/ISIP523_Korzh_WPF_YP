<UserControl x:Class="Main.Views.FrozenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <Border Background="#FFEEEE" BorderBrush="#FFAAAA" BorderThickness="1"
                CornerRadius="8" Padding="24" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="❄ Аккаунт заморожен" FontSize="20" FontWeight="Bold"
                           Foreground="Red" Margin="0,0,0,8"/>
                <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Margin="0,0,0,16"/>
                <Button Content="Оспорить заморозку" HorizontalAlignment="Left"
                        Command="{Binding AppealFreezeCommand}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CanAppeal}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <TextBlock Foreground="Gray" FontStyle="Italic">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CanAppeal}" Value="False">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                    <Run Text="Заявка на разморозку уже отправлена"/>
                </TextBlock>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>